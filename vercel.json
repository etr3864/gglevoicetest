{
  "buildCommand": "pnpm --filter @voice/shared build && pnpm --filter @voice/web build",
  "outputDirectory": "apps/web/dist",
  "installCommand": "pnpm install",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
